name: Melon Ticket Actions

on:
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Run Melon Ticket Script
        uses: mooyoul/melon-ticket-actions@master
        with:
          entryPoint: reserve
          args: >
            --product-id 211635
            --message "抢票测试"

